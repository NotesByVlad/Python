# ---------------------------------- Constructors Exercises ---------------------------------------------------------- #

# 1. Create a class `Book` with attributes `title` and `author`. Add a constructor to initialize these attributes.
class Book:
    def __init__(self, title, author):
        self.title = title
        self.author = author
# TODO 1. Create a Book object with `title='1984'` and `author='George Orwell'`.----------------------------------------



# 2. Create a class `Circle` with an attribute `radius` and initialize it using a constructor.
class Circle:
    def __init__(self, radius):
        self.radius = radius
# TODO 2. Create a Circle object with `radius=5`. Access the radius attribute.------------------------------------------

